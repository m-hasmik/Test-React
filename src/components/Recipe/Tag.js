import styled from 'styled-components'

import CommonTag from '~/components/common/Tag'

export const Tag = styled(CommonTag)`
  bottom: auto;
  left: 0.5rem;
  position: absolute;
  right: auto;
  top: 0.5rem;
`
